apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: strip-ranker-prefix
spec:
  stripPrefix:
    prefixes:
      - /api/ranker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ranker-ingress
  annotations:
    kubernetes.io/ingress.class: "traefik-internet"
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.middlewares:
      default-strip-ranker-prefix@kubernetescrd,
      kube-system-http-redirect-https@kubernetescrd,
      kube-system-http-redirect-https-testing@kubernetescrd,
      kube-system-common-headers@kubernetescrd

spec:
  tls:
    - hosts:
        - "*.hk-testing.web3eye.io"
        - hk-testing.web3eye.io
      secretName: hk-testing-web3eye-io-tls
  rules:
    - host:
      http:
        paths:
          - path: /api/ranker
            pathType: Prefix
            backend:
              service:
                name: ranker
                port:
                  number: 30140
